---
description: 项目技术选型（Vue3 + Tailwind + Lucide）
alwaysApply: true
---

# 项目技术选型

- 核心框架：Vue 3，采用响应式、模型驱动和组件化开发（状态存储于响应式模型中；渲染逻辑声明式书写）。
- UI 布局与样式：Tailwind CSS（优先使用语义化、工具优先的类组合，并依赖 Tailwind 的编译流程）。
- 图标系统：采用 `lucide-vue-next` 作为图标库（轻量级、基于组件）。

# 实践约束

- 在布局、间距、排版等方面优先使用 Tailwind 类，而不是临时/自定义 CSS。
- 除非有特殊需求，否则优先使用 `lucide-vue-next` 图标组件，而非其他图标库。
- 增加新功能时，新代码默认与上述技术栈保持一致。
